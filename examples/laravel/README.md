## Laravel

Primitive integration Porter to Laravel project.

## Installation

1. Put this files to folder (e.g. `/websocket`) in root of Laravel project.
2. `echo 'IS_SERVER=false' >> .env` (on server `IS_SERVER=true` 🙂).

## Run
```bash
php websocket/server.php start
```
